name: CI/CD

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install
      run: pip install flask google-generativeai

    - name: Test
      run: echo "Testing OK"

    - name: Build Docker
      run: docker build -t pylearn-ai .

    - name: Deploy
      run: echo "Deploy ke cloud"